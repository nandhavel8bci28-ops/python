def split_and_join(line):
    # 1. Split the string on space (" ")
    words = line.split(" ")
    
    # 2. Join the list of words using a hyphen ("-")
    result = "-".join(words)
    
    return result
